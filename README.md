# HRL — Hue, Reach, Level

A color space built around a regular hue bicone: black, white, and vivid in every equilateral hue triangle.

## Current v2 release: HRL v2 Beta 1

**The selected 0.13 metric-leaning checkpoint (`metric-b2`), now the v2 default.** This is a promotion, not a new fit. One shared learned Reach/Level bank operates over native sRGB and the repaired full physical-reference domain.

[Current release](https://gaycoonie.github.io/HRL/v2/) · [Beta 1 picker](https://gaycoonie.github.io/HRL/v2/beta1.html) · [Full technical write-up](v2/HRL_v2_Beta_1.md) · [Readable web edition](https://gaycoonie.github.io/HRL/v2/beta1-notes.html) · [Benchmarks](https://gaycoonie.github.io/HRL/v2/benchmarks.html) · [All files](https://gaycoonie.github.io/HRL/library.html)

```js
import {createHRLv2} from './v2/index.mjs';
const hrl = await createHRLv2(); // native sRGB; HRL v2 Beta 1
const q = hrl.fromRGB([0.2, 0.5, 0.8]);
const rgb = hrl.toRGB(q);
const full = await createHRLv2({gamut:'full'});
const audit = full.importXYZ(hrl.toXYZ(q));
```

The [release manifest](v2/default.json) pins the exact coefficient and boundary hashes. Normal retained-pair weighted COMBVD is **29.107048** for 3,331 native sRGB pairs and **29.948555** for 3,813 full-domain pairs. These fitted development populations are not interchangeable with all-input mapped ColorBench; the [original report](v2/research/boundary-tonal/results/REPORT.md) preserves both.

## Release 1 remains available

The [root picker](https://gaycoonie.github.io/HRL/) and `src/index.mjs` remain **R15-D Release 1**. The package's existing root export is unchanged. Its native sRGB and Rec.2020 implementation, coefficients, tests, and historical documentation are preserved.

```js
import {createHRL} from './src/index.mjs';
const release1 = await createHRL({gamut:'srgb'});
const wideRelease1 = await createHRL({gamut:'rec2020'});
```

[Complete pre-beta README](README.pre-beta1.md) · [Release 1 source](src/index.mjs)

## Geometry and units

Public coordinates satisfy `0 <= R <= L <= 1`; H is cyclic degrees. Shares are chromaticness R, whiteness L−R, and blackness 1−L. Level is inverse blackness, not a synonym for luminance.

The regular-bicone embedding is `[L-R/2, sqrt(3)/2*R*cos(H), sqrt(3)/2*R*sin(H)]`, with H converted to radians inside the trigonometric functions. Distance is Euclidean **in that embedding**, not in raw H/R/L.

## GitHub Pages and local use

The repository is served directly at **https://gaycoonie.github.io/HRL/**, from `main` at the repository root. The site is static; no account, framework, CDN, or backend is needed to use the pickers.

For local browser use, retain the full repository layout and serve it over HTTP:

```sh
git clone https://github.com/GayCoonie/HRL.git
cd HRL
python3 -m http.server 8000
# Open http://localhost:8000/v2/ for the current release.
# Open http://localhost:8000/ for Release 1.
```

The reference modules also work in Node 18 or later. The modular pickers need HTTP rather than a `file:` page; preserved standalone research viewers keep their embedded model data.

## Research and reproducibility

[Chronological archive](https://gaycoonie.github.io/HRL/v2/archive.html) · [Searchable complete file catalogue](https://gaycoonie.github.io/HRL/library.html) · [Preserved four-way 0.13 comparison](https://gaycoonie.github.io/HRL/v2/boundary-tonal.html)

No earlier page, Markdown document, JSON record, or numerical model is removed. Markdown has both its original raw file and a generated reading edition where a same-name HTML page does not already exist. Navigation is generated by `site/build.py`; the publication workflow checks retained source identities, release-wrapper equivalence, links, and browser behavior.

```sh
npm test                         # existing repository tests
node site/test-beta1.mjs          # release-wrapper/model identity checks
python -m pip install mistune
python site/build.py              # rebuild navigation, catalogue, reading editions
python site/check.py              # preservation and new-site link audit
```

The older `v2/lib/index.mjs` retains its historical BASR/linear factory. Use **`v2/index.mjs`** for the named Beta 1 release. No npm version or historical checkpoint is silently reinterpreted.
