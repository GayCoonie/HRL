# Final independent evidence review — HRL v2 tonal continuation

**Contract: confirmed for C1–C4 within the scopes below. Quality: confirmed for the reported numerical conclusions, with no blocking discrepancy. C5 publication and durable delivery remain pending the root agent's external verification.**

All six candidates are ineligible under the fixed rule. The largest native regular-blue improvement is `sheet-raw`, **0.744575980165%**, below the required 5%. `selectedCandidate: sheet-raw` is a displayed research comparison, not a promotion. Beta 1 remains frozen. H1 is supported at sampled points, H2 is rejected for these six executed trials, and H3 is supported within the sampled contracts.

This is a fresh read-only review by an agent separate from the fitter and report author, with an additional independent conditioning reviewer. No fit, complete runtime audit, full `npm test`, source edit, publication, or frozen-receipt rewrite was performed. New outputs were written only in this review directory.

## Criterion verdicts

| Criterion | Verdict | Evidence and practical limit |
|---|---|---|
| C1 — Baseline tracer | Confirmed | Fresh Beta 1 retained/native-mapped scores, public-versus-explicit-record factory identity, boundary roundtrips and H275 XYZ sheet export; input/cache population and mask identities verified. This establishes the exercised cases, not a whole-domain proof. |
| C2 — Conditioning diagnosis | Confirmed | Source-bound same-point saved arithmetic for all six candidates, prior independent baseline/first-pair evidence, and fresh 80-digit scalar-map plus actual-JavaScript finite differences at both final-candidate maxima. Scope is fixed-H, 2×2 common-coordinate conditioning, not XYZ/perceptual conditioning. |
| C3 — Completed candidate round | Confirmed | Six full arrays and traces match their versioned fitter hashes; all six actual-runtime receipts reaggregate to the published comparison; shared-bank records, preserved sources, populations, geometry receipts and bounded runtime probes agree. Candidate eligibility itself is rejected for all six, as reported. |
| C4 — Independent review | Confirmed | Current source identities and full comparison arithmetic checked; meaningful runtime and derivative evidence reproduced; report read completely; stale selector expectation resolved by exact source reconstruction and paired reducer execution. |
| C5 — Delivery/publication | Not assessed here; pending | This review verifies scientific evidence and report claims, not the current remote branch, deployment, browser behavior, or durable deliverables. |

## Fresh executions

The source workspace during these checks was `/workspace/scratch/20abb8fd27ac/hrl-v2-next`; scratch receipts were in its sibling `hrl-optimization`. The output directory was `/workspace/scratch/13a5381bdd70/review`.

1. **Six-candidate raw reaggregation: exit 0.**

```sh
python /workspace/scratch/20abb8fd27ac/hrl-optimization/review-runtime-candidates.py \
  --candidate /workspace/scratch/20abb8fd27ac/hrl-optimization/audit-mild.json \
  --candidate /workspace/scratch/20abb8fd27ac/hrl-optimization/audit-conditioned.json \
  --candidate /workspace/scratch/20abb8fd27ac/hrl-optimization/audit-sheet-raw.json \
  --candidate /workspace/scratch/20abb8fd27ac/hrl-optimization/audit-sheet-robust.json \
  --candidate /workspace/scratch/20abb8fd27ac/hrl-optimization/audit-blue-raw.json \
  --candidate /workspace/scratch/20abb8fd27ac/hrl-optimization/audit-blue-robust.json \
  --comparison /workspace/scratch/20abb8fd27ac/hrl-v2-next/v2/research/tonal-next/results/comparison.json \
  --out /workspace/scratch/13a5381bdd70/review/final-reaggregation.json
```

Decisive output: `status: passed`, `comparisonStatus: no-eligible-candidate`. Recomputed all regular/critical/control path summaries and historical sheet aggregates, all selection values, ten guards per candidate, record/source identities, shared-bank record structure and saved geometry-contract counts. The comparison's seven input hashes match its actual audit bytes. All six fail the 5% blue requirement; `smooth-conditioned` additionally fails four path guards.

2. **Fresh actual-runtime reproduction: exit 0.**

```sh
node /workspace/scratch/13a5381bdd70/review/runtime-fresh.mjs > /workspace/scratch/13a5381bdd70/review/runtime-fresh.log 2>&1
```

The reviewer probe uses actual model factories, its own weighted/unweighted STRESS reduction and the direct GenSpace sheet stencil. It exercised Beta 1, `sheet-raw`, `blue-raw` and `blue-robust`, each in native and full realizations. All retained and separate mapped scores agreed with the receipts within the asserted 1e-8 tolerance. At H281, all 20 path CVs from 257-point paths matched exactly, and all 121 sheet values agreed within **2.274e-13**. Across **960** finite boundary/near-black/neutral/vivid cases, maximum embedding roundtrip error was **7.327e-12**. All **240** public-Beta1-versus-explicit-record XYZ comparisons were exactly equal. H275 direct-runtime XYZ grids are included in `runtime-fresh.json` (45 points per profile).

Fresh weighted baseline scores were native retained **29.107047807815334** over 3,331 pairs; full retained **29.948554645320474** over 3,813 pairs; and native mapped/all-input **34.489196386295546** over 3,813 pairs, including 482 mapped pairs. These populations were never mixed for eligibility.

3. **Source, population, trace and cache integrity: exit 0.**

```sh
python /workspace/scratch/13a5381bdd70/review/integrity-check.py
```

Decisive output: `status: passed`, 148 frozen files, six candidate records, 16 raw-diagnostic gzip archives. The six trace lengths equal the recorded closure counts: 460, 484, 464, 506, 622 and 628. Each record's fitter hash matches the preserved first-, second- or final-round source. Every record has one bank and seven layers, with the frozen metric-b2 guard parent. Both augmented cache files match their recorded full hashes and preserve the complete original 85,821,696-byte prefix byte-for-byte. The native mask hash is `c95b0a38cd038fff9163f0d38ed4924addb95b1156283cf4a2ed65909efa2251`; cache indices match that mask and the full 0–3812 population.

The script also executed the following command in the original repository, **exit 0**, with no diff:

```sh
git diff --exit-code bc70cc754e83c92790a88e0833ca8956d4d1407e -- v2/index.mjs v2/research/boundary-tonal v2/research/shared-rl v2/research/relative-domain v2/lib src
```

4. **Conditioning: both commands exit 0; nested Node exit 0.**

```sh
/workspace/scratch/20abb8fd27ac/hrl-opt-env/bin/python /workspace/scratch/20abb8fd27ac/hrl-optimization/review-conditioning-blue-reproduce.py > /workspace/scratch/13a5381bdd70/review/conditioning-fresh.json
/workspace/scratch/20abb8fd27ac/hrl-opt-env/bin/python /workspace/scratch/20abb8fd27ac/hrl-optimization/review-conditioning-blue-check.py > /workspace/scratch/13a5381bdd70/review/conditioning-all-six-fresh.json
```

The unchanged scalar forward-map reproduction uses 80-digit arithmetic and central differences at two extremely small steps; it does not import the analytic chain rule. Final maxima reproduce as **114567.439790725** (`blue-raw`) and **113643.042801774** (`blue-robust`), both at H269, R=.000999, L=.001. These worsen Beta 1's sampled maximum by **0.987471%** and **0.172645%**. Fresh stable JavaScript relative errors were at most **9.373e-7**. All-six saved arithmetic reproduced exactly across **1,950 rows and 13,650 matrices**, with matching points, controls, record and source hashes. This last check reaggregates stored matrices; it does not regenerate their whole derivative grid. See `conditioning-review.md` for complete scope and hashes.

## The selector failure is resolved, not suppressed

The unchanged supplemental reviewer was rerun through a wrapper that redirected only its output destination:

```sh
/workspace/scratch/20abb8fd27ac/hrl-opt-env/bin/python /workspace/scratch/13a5381bdd70/review/rerun-supplemental.py > /workspace/scratch/13a5381bdd70/review/supplemental-fresh.log 2>&1
```

It correctly returned **exit 1**, preserving the same single `selector_unchanged` failure among **267,757 checks**. All other hash checks passed; maximum scaled arithmetic difference was **5.737e-16**. The wrapper leaves the original review script and original failure receipt unchanged. Its log prints the original destination label, but the actual new output is `supplemental-fresh.json`.

The expected selector SHA-256 was `c5cfd3a6c6fd7970845c404fec5d8461b2228702f0284a8daff627071680e9b6`. Reconstructing the prior file by replacing the current rejection prose “No research candidate meets” with “Neither research candidate meets” produces **that exact hash**. The actual current selector is `d4dbec95546c4d252a8143768e15a576437b72319f88cb791b7057a75a9ce8f4`. The exact source diff, retained as `selector-source.diff`, has no computational or threshold change.

Both selector versions were executed against the same seven final receipts, **exit 0 each**. All output fields except the timestamp and this rejection sentence were exactly equal, including model data, all guards, status, selected candidate and input hashes. Full exact command arrays and verdict are in `selector-resolution.json`; both outputs and the byte-exact reconstructed source remain attached. This establishes a stale prose-sensitive identity expectation, not a failed scientific gate. The original failed receipt must remain a failure receipt with this resolution alongside it.

## Report and remaining limitations

The entire final `RESULTS.md` was read, including its appended final-review paragraph and selector resolution. The reviewed bytes have SHA-256 **`81dbb0ec11d90783d61eb7d99262fdf63c3d8c47123455630bf690c514092bb7`**. It accurately rejects all six candidates, separates populations, distinguishes training data from validation, discloses extreme-edge/worst-path regressions, and limits conditioning and geometry conclusions to finite samples. No blocking report discrepancy was found.

One nonblocking quality finding remains: **coverage, low severity, high confidence**, `conditioning-audit.py` lines 170–180. Its implementation gate checks forward parity, analytic/autograd consistency, propagated formulas and positive determinants, but does not itself gate numerical-JavaScript derivative error. Consequently, exit 0 alone would be insufficient for C2. Explicit multistep inspection and fresh independent reproduction close that gap for this report. A future reusable gate could make a documented stable-step agreement requirement executable; no fix was made during this review.

No browser, deployment, held-out-observer, universal smoothness, continuous-domain no-fold, or full XYZ/perceptual-conditioning claim is verified here. The source fitters were not rerun. The fitting-wall-time history is taken from preserved execution evidence rather than independently replayed.

After these checks finished, a workspace/environment transition removed the original `/workspace/scratch/20abb8fd27ac` source tree. The new review artifacts listed here survived. Root reported restoring the 79 uploaded experiment files under `/workspace/scratch/13a5381bdd70/hrl-v2-next` from the already-uploaded GitHub blobs; that restoration is subsequent to the executions above. The historic absolute paths inside receipts remain provenance, not promised current locations. This review stays bound to its recorded hashes and the reviewed report hash; root must match restored/published bytes and complete C5. No numerical check was claimed to run against the vanished path after the transition.

Key evidence identities: final `comparison.json` **`16db06959b0213bc441006e0fdfada37608ae8e6edc892b0cb2c0b458dfb77fb`**; frozen Beta 1 release definition **`92aa2f9647b406239dd52cd22feed61794f3d6ed74a1c33ba9361516cff3bb72`**. Candidate, source, cache, trace and raw-audit hashes are retained in `integrity-fresh.json`, `final-reaggregation.json`, `runtime-fresh.json`, and the conditioning receipts. Any substantive source or metric change requires a new review of the affected claims.
